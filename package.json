{
  "name": "jp-music-lyrics-jlpt",
  "description": "A package for scraping Japanese music lyrics and converting them to JLPT levels.",
  "version": "1.0.0",
  "scripts": {
    "it": "node server_serachEngine.js",
    "test": "(eslint lib || true) && jest",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Oramaak/JP-Music-Lyrics-JLPT.git"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "puppeteer": "^24.11.1"
  }
}
